{
	"name": "@better-svelte-email/cli",
	"description": "CLI for better-svelte-email",
	"version": "1.4.0",
	"scripts": {
		"dev": "bun run build:assets && bun --watch src/index.ts dev -d ../../apps/docs/src/lib/emails -c ../../apps/docs/src/app.css",
		"build:preview": "bun run --cwd ../preview-server build",
		"build:cli": "tsdown",
		"build:assets": "bun run ./scripts/copy-preview-assets.ts",
		"build": "bun run build:preview && bun run build:cli && bun run build:assets",
		"lint": "prettier --check . --ignore-path ../../.prettierignore && eslint src",
		"test": "vitest run"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Konixy/better-svelte-email.git",
		"directory": "packages/cli"
	},
	"module": "./dist/index.mjs",
	"type": "module",
	"main": "./dist/index.cjs",
	"bin": {
		"bse": "./dist/index.cjs"
	},
	"files": [
		"dist"
	],
	"publishConfig": {
		"access": "public"
	},
	"exports": {
		".": {
			"import": "./dist/index.mjs",
			"require": "./dist/index.cjs"
		},
		"./package.json": "./package.json"
	},
	"dependencies": {
		"@better-svelte-email/server": "workspace:*",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"commander": "^14.0.3",
		"svelte": "^5.53.7",
		"vite": "^7.3.1"
	}
}
